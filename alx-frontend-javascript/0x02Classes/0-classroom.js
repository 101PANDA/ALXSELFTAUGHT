// Class named Classroom:  accept one attribute named maxStudentsSize (Number) and assigned to _maxStudentsSize

export default class Classroom {

    constructor(maxStudentSize) {
        this._maxStudentsSize = maxStudentSize;
    }
    
}